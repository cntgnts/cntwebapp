pipeline {
  
  agent any 
  
  environment {
  
    name = 'kamal'
    
    }
  
  stages {
    stage('build') {
    
      steps {
        
        echo "${env.name}"
      
      }
    
    }
  }


}
